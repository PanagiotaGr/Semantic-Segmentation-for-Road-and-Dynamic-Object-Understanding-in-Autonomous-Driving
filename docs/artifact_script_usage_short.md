# Short Usage

Run:

`python scripts/generate_uncertainty_artifacts.py --input outputs/sample_logits.pt --output-dir outputs/uncertainty --input-type logits`

Then inspect `metadata.json` and the tensors under `sample_0000/`.
