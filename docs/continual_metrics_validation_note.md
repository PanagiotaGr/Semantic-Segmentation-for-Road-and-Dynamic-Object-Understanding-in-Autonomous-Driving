# Continual Metrics Validation Note

The continual-learning metric utilities were added with documentation and configuration.

A follow-up PR should add unit tests for:

- average accuracy,
- forgetting score,
- retained performance,
- forward transfer,
- compact metric reports.

The initial test-file creation was blocked by the connector, so validation is documented here for follow-up.
